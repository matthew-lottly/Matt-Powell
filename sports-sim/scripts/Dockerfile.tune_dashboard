FROM python:3.13-slim-bookworm
WORKDIR /app
COPY . /app
RUN pip install --no-cache-dir streamlit
EXPOSE 8501
CMD ["streamlit", "run", "scripts/tune_dashboard.py", "--server.port", "8501", "--server.address", "0.0.0.0"]
