# Example Output

Representative GeoPackage layer inventory exported for QGIS review.

```text
output_file: operations_workbench_demo.gpkg
layers:
  - stations_current_status (42 features)
  - inspection_routes (7 features)
  - district_bookmarks (5 features)
layout_templates:
  - station_brief_a4
  - district_overview_a3
```

This is the kind of deliverable a GIS reviewer can understand immediately: one package, several ready-to-open layers, and expected print layouts.