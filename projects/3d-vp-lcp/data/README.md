# Data

This directory holds LiDAR tiles (LAS/LAZ) and auxiliary layers.

Generated files (e.g. `sample_lidar.las`) are excluded from version control.

To create the sample dataset:

```bash
python -m vp_lcp.scripts.generate_sample_data
```
